import { FlTrancatorPipe } from './fl-trancator.pipe';

describe('FlTrancatorPipe', () => {
  it('create an instance', () => {
    const pipe = new FlTrancatorPipe();
    expect(pipe).toBeTruthy();
  });
});
