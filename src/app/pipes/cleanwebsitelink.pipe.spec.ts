import { CleanwebsitelinkPipe } from './cleanwebsitelink.pipe';

describe('CleanwebsitelinkPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanwebsitelinkPipe();
    expect(pipe).toBeTruthy();
  });
});
