import { InvestneedPipe } from './investneed.pipe';

describe('InvestneedPipe', () => {
  it('create an instance', () => {
    const pipe = new InvestneedPipe();
    expect(pipe).toBeTruthy();
  });
});
